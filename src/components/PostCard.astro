---
const { post } = Astro.props;
---
<a href={`/post/${post.id}`} class="block border rounded p-4 hover:bg-gray-100">
  <h2 class="text-xl font-bold">{post.titulo}</h2>
  <p>{post.resumo}</p>
  <span class="text-sm text-gray-500">Autor: {post.autor}</span>
</a>
