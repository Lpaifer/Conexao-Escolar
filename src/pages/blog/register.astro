---
import Header from '../../components/Header.astro';
import Layout from '../../layouts/Layout.astro';
---

<Layout title="REGISTRO | CONEXÃO ESCOLAR">
  <Header />

  <script type="module">
    document.addEventListener('DOMContentLoaded', () => {
      const form = document.querySelector('form');
      form.addEventListener('submit', async (e) => {
        e.preventDefault();

        const email = form.querySelector('input[type="email"]').value;
        const senha = form.querySelector('input[type="password"]').value;

        const res = await fetch('https://conexao-backend.onrender.com/api/auth/registro', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email, senha })
        });

        const data = await res.json();

        if (res.ok) {
          alert('Conta criada com sucesso! Faça login agora.');
          window.location.href = '/blog/login';
        } else {
          alert(data.msg || 'Erro ao registrar usuário.');
        }
      });
    });
  </script>

  <form class="max-w-md mx-auto mt-24 bg-white dark:bg-gray-900 text-gray-900 dark:text-white p-8 rounded-2xl shadow-xl space-y-5">
    <h2 class="text-2xl font-bold text-center">Criar uma nova conta</h2>
    <p class="text-center text-sm text-gray-500 dark:text-gray-400">É rápido e prático.</p>

    <div class="flex gap-3">
      <input type="text" placeholder="Nome" class="w-full p-3 border border-gray-300 dark:border-gray-700 rounded-lg mt-2" />
    </div>

    <input type="email" placeholder="Email" class="w-full p-3 border border-gray-300 dark:border-gray-700 rounded-lg" required />

    <input type="text" placeholder="Celular" class="w-full p-3 border border-gray-300 dark:border-gray-700 rounded-lg" />

    <div>
      <label class="block mb-1 text-sm font-semibold">Data de nascimento</label>
      <input type="date" name="data_nascimento" class="w-full p-3 border border-gray-300 dark:border-gray-700 rounded-lg" required />
    </div>

    <div>
      <label class="block mb-1 text-sm font-semibold">Gênero</label>
      <div class="flex justify-between">
        <label class="flex items-center gap-2 border rounded-lg p-2 w-1/3">
          <input type="radio" name="genero" value="feminino" />
          Feminino
        </label>
        <label class="flex items-center gap-2 border rounded-lg p-2 w-1/3">
          <input type="radio" name="genero" value="masculino" />
          Masculino
        </label>
        <label class="flex items-center gap-2 border rounded-lg p-2 w-1/3">
          <input type="radio" name="genero" value="personalizado" />
          Personalizado
        </label>
      </div>
    </div>

    <input type="password" placeholder="Nova senha" class="w-full p-3 border border-gray-300 dark:border-gray-700 rounded-lg mt-2" required />

    <p class="text-xs text-gray-500 dark:text-gray-400">
      As pessoas que usam nosso serviço podem ter carregado suas informações de contato dentro do nosso blog. <a href="#" class="text-blue-600 dark:text-blue-400 underline">Saiba mais.</a>
    </p>

    <p class="text-xs text-gray-500 dark:text-gray-400 mt-2">
      Ao clicar em Cadastre-se, você concorda com nossos <a href="#" class="underline text-blue-600 dark:text-blue-400">Termos</a>, <a href="#" class="underline text-blue-600 dark:text-blue-400">Política de Privacidade</a> e <a href="#" class="underline text-blue-600 dark:text-blue-400">Política de Cookies</a>.
    </p>

    <button type="submit" class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 rounded-xl text-lg">
      Cadastre-se
    </button>

    <p class="text-center mt-2 text-sm">
      <a href="/blog/login" class="text-blue-600 dark:text-blue-400 hover:underline">Já tem uma conta?</a>
    </p>
  </form>
</Layout>
